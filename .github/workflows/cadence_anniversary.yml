name: Cadence: anniversary QC
on:
  schedule:
    - cron: '0 3 24 8 *'   # Aug 24, 03:00 UTC
  workflow_dispatch:
permissions:
  contents: read
jobs:
  anniversary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Anniversary QC
        shell: bash
        run: |
          set -euo pipefail
          echo "[anniversary] QC task placeholder"
