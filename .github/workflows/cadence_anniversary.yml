name: Cadence: anniversary QC + recovery
on:
  schedule: [{ cron: "0 3 24 8 *" }] # Aug 24, 03:00 UTC
  workflow_dispatch:
permissions: { contents: read }
jobs:
  anniversary:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: |
          set -eu
          echo "Anniversary QC task placeholder"
