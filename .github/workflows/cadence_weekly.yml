name: Cadence: weekly QC + recovery
on:
  schedule: [{ cron: '0 3 * * 1' }]
  workflow_dispatch:
permissions: { contents: read }
jobs:
  weekly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Weekly QC
        shell: bash
        run: |
          set -euo pipefail
          echo "[weekly] QC placeholder"
