name: Cadence: weekly QC + recovery

on:
  schedule:
    - cron: "0 3 * * 1"      # Every Monday 03:00 UTC
  workflow_dispatch:

permissions:
  contents: read

jobs:
  weekly:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Weekly QC
        run: |
          set -eu
          echo "[weekly] QC task placeholder"
