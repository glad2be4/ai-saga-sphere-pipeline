name: Cadence — Weekly Excerpt
on: { schedule: [ { cron: '5 9 * * 1' } ], workflow_dispatch: {} }
jobs: { run: { runs-on: ubuntu-latest, steps: [ { uses: actions/checkout@v4 }, { run: sudo apt-get update -y && sudo apt-get install -y jq }, { run: CADENCE_MODE=weekly REPO_SLUG="${{ github.repository }}" FEED_URL="${{ vars.FEED_URL }}" bash scripts/cadence.sh } ] } }
